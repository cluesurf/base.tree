note <UUID generation. The clean uniform interface; the per-env native implementation is selected at build time and hidden. Random by nature, so this is an explicit escape hatch, never used in the deterministic language core.>

load @cluesurf/base/code/native/uuid
  find generate-v4

task version4
  like text
  send back
    call generate-v4

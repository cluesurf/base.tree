
task get
  like text

  send back
    call browser-get-pathname

task set
  take path, like text

  call browser-set-pathname
    read path

task browser-get-pathname
  mark private
  dock call <location.pathname>

task browser-set-pathname
  mark private
  take path
  dock call <history.pushState(null, "", path)>

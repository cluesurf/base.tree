
note <Node.js environment implementation. Internal: reached only through the public environment API.>

dock load
  load <global:process>, name process

task current-directory
  like text
  send back
    call process/cwd
